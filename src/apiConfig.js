// Define the base API URL in one place
const API_BASE_URL = process.env.REACT_APP_API_BASE_URL || 'http://localhost:8080';

// Export the constant so it can be imported and used elsewhere
export default API_BASE_URL;
